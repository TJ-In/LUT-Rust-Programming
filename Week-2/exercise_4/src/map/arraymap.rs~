pub fn draw_map(pos: (usize, usize)) {
    let mut matrix = [["*"; 5]; 5];
    matrix[pos.0][pos.1] = "x";
    for row in matrix {
        let mut vec = vec![];
        for block in row {
            vec.push(block);
        }
        println!("{}", vec.join(" "));
    }
}
